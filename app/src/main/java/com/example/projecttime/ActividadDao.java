package com.example.projecttime;

/*
import androidx.room.Dao;
import androidx.room.Insert;
import androidx.room.Query;

import com.example.projecttime.conexion.Actividad;

import java.util.List;

@Dao*/
public interface ActividadDao {
   /* @Query("SELECT * FROM actividad")
    List<Actividad> getAll();

    @Query("SELECT * FROM actividad WHERE nombre LIKE :nombre LIMIT 1")
    Actividad finByName(String nombre);

    @Insert
    Long insert (Actividad actividad);
    //void insertAll(Actividad actividad);

*/



}
